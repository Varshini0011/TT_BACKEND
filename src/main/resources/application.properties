spring.application.name=BE
server.port=${PORT:8080}

# PostgreSQL Database
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Mistral API
mistral.api.key=${MISTRAL_API_KEY}

# Allow large image uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Static file serving
spring.web.resources.static-locations=file:uploads/,classpath:/static/
```

## Then add these in Render → Environment:
```
DB_URL = jdbc:postgresql://dpg-d74cn04r85hc73fo062g-a/tt_postgres
DB_USERNAME = tt_postgres_user
DB_PASSWORD = Rdj45E6FjEto8pSpHR6QORtO9r9ZVpIx
MISTRAL_API_KEY = your_mistral_key
```

## Also add `.gitignore` in BE folder:
```
target/
application.properties