spring.application.name=BE

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://dpg-d74cn04r85hc73fo062g-a/tt_postgres
spring.datasource.username=tt_postgres_user
spring.datasource.password=Rdj45E6FjEto8pSpHR6QORtO9r9ZVpIx
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Mistral API
mistral.api.key=${MISTRAL_API_KEY}

server.port=8080

# Allow large image uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Static file serving for uploaded worker proof images
spring.web.resources.static-locations=file:uploads/,classpath:/static/